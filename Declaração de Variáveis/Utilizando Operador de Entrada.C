#include <iostream>

using namespace std;

// Não esquecer do ponto em vírgula fora das aspas

int main()
{ 

 //testa a entrada de números inteiros, decimais e caracteres:
 
 int m,n; // declarando variáveis inteiras
 cout << "insira dois números inteiros:"; // mensagem na tela
 
 cin >> m >> n; // operador de entrada
 
 cout << "m = " << m << "n = " << n << endl;
 
 double x,y,z; // declarando variáveis flutuantes
 
 cout << "Insira tres numeros decimais:"; ;
 
 cin >> x >> y >> z; 
 
 cout << "x = " << x << "y = " << "z =" << z << endl;
 
  char c1, c2 ,c3, c4; // declarando variáveis como caracteres
  
  cout << "Insire quatro caracteres:"; 
  
   cin >> c1 >> c2 >> c3 >> c4;

   cout << "c1 = " << c1 << "c2 = " << "c3 =" << c3 << "c4 =" << c4 << endl;
   
}
  