#include<iostream>

using namespace std;

int main( )

{ 
  int a, b, subtraçao; // Declarando as variáveis
  cout << "Insira o primeiro valor" << endl; // mensagem na tela
  cin >> a >> endl;
  cout << "Insira o segundo valor" << endl;  
  cin >> b >> endl;
  cout << "A diferença é" << endl;
  subtraçao = a - b
  cout << "A diferença é:" << subtraçao << endl;
  
  return 0; // diz que o programa terminou bem
 } 
  

